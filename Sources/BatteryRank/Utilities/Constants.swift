import Foundation

enum Constants {
    static let appName = "BatteryRank"
    static let bundleIdentifier = "com.batteryrank.app"
    static let defaultPollingInterval: TimeInterval = 30 // seconds
    static let aggregationInterval: TimeInterval = 300   // 5 minutes
    static let defaultRetentionDays = 7
}
